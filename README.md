# passwordManagerApi
Server side logic for passwordManagerGui


.env file, database connection string and key generated I obviously cannot post on github because of security, but all of those are passed in to Render which handles deployments of my application for me. This repo of course has all the backend logic that is used interact with my MongoDB database.
